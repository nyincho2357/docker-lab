{
  "name": "Docker Lab Dev Container",
  "build": {
    "dockerfile": "../Dockerfile"
  },
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "extensions": [
    "ms-azuretools.vscode-docker"
  ],
  "postCreateCommand": "echo 'Devcontainer is ready. Run docker commands inside!'"
}
